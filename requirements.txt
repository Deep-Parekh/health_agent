# Runtime deps for the FastAPI backend (Modal image installs from this file).
# gradio is retained only for the legacy app.py; drop it when that is retired.
duckduckgo-search>=4.1.0
fastapi>=0.115
uvicorn[standard]>=0.30
gradio>=5.23.1
langchain>=1.1.2
langchain-core>=1.1.1
langchain-openai>=0.0.5
langgraph>=1.0.4
psycopg[binary]>=3.1
pydantic>=2.12.5
python-dotenv>=1.2.1
